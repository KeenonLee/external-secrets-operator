apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "externalsecrets.fullname" . }}-test-connection"
  labels:
    {{- include "externalsecrets.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "externalsecrets.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
