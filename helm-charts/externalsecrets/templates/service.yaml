apiVersion: v1
kind: Service
metadata:
  name: {{ include "externalsecrets.fullname" . }}
  labels:
    {{- include "externalsecrets.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "externalsecrets.selectorLabels" . | nindent 4 }}
